static {
        String[] morse = {".-", "-...", "-.-.", "-..", ".", "..-.", "--.", "....", "..", ".---", "-.-", ".-..", "--", "-.", "---", ".--.", "--.-", ".-.", "...", "-", "..-", "...-", ".--", "-..-", "-.--", "--.."};
        for (int i = 0; i < 26; i++) {
            char letter = (char) ('A' + i);
            TO_MORSE.put(letter, morse[i]);
            FROM_MORSE.put(morse[i], letter);
        }
    }